---
statsBar:
  items:
  - text: ui.hopeMeter
    side: left
    updateTrigger:
      variables: ["player.hope"]
    conditionalStyles:
    - condition: player.hope > 40
      styleClasses: ["normal"]
    - condition: player.hope <= 40 && player.hope > 20
      styleClasses: ["warning"]
    - condition: player.hope <= 20
      styleClasses: ["critical"]
  - text: ui.computeMeter
    side: left
    updateTrigger:
      variables: ["player.compute"]
    conditionalStyles:
    - condition: player.compute >= 4
      styleClasses: ["normal"]
    - condition: player.compute < 4 && player.compute >= 2
      styleClasses: ["warning"]
    - condition: player.compute < 2
      styleClasses: ["critical"]
  - text: ui.citationMeter
    side: right
    updateTrigger:
      variables: ["player.citations"]
  - text: ui.timeMeter
    side: right
    updateTrigger:
      variables: ["year", "month"]
messageWindow:
  typewriterSpeed: 30
itemList:
  title: ui.items
  raritySpecificStyles:
    - minRarity: 10
      styleClasses: ["r_legendary"]
    - minRarity: 6
      maxRarity: 9
      styleClasses: ["r_rare"]
    - minRarity: 3
      maxRarity: 5
      styleClasses: ["r_uncommon"]
statusList:
  title: ui.status
footer:
  preamble:
  buttons:
    - text: "English"
      setLanguage: en
    - text: "中文"
      setLanguage: zh
    - text: ui.helpButton
      messageTitle: ui.helpTitle
      message: ui.helpContent
      confirmText: ui.ok
    - text: ui.aboutButton
      messageTitle: ui.aboutTitle
      message: ui.aboutContent
      confirmText: ui.ok
    - text: ui.seedButton
      messageTitle: ui.seedTitle
      message: ui.seedContent
      confirmText: ui.ok
    - text: ui.github
      url: https://github.com/morriswmz/phd-game
